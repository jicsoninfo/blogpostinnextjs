/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            // "images.example.com",
        ]
    }
}

module.exports = nextConfig
